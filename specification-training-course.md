I have been writing a javascript application that trains a small AI on a dataset of 2 or 3 thousand images 16-bit black and white. From this sample dataset, the trained AI will be able to classify new images (for example "is this a face?").
I would like to make this project into a website where people can come and learn how AIs are created and how they work.
There will be short explanations and interactive parts. The interactive parts will let users load and browse existing datasets, and also modify them and make their own to see their effets.
The training phase takes on average 20 minutes at the moment, but I would hope that during the training I could optimize this or at least present other details ... ideally running the training in real-time but for now I think this type of performance is not possible in desktop or phone browsers.
After the training phase, the AI model will be ready and can be used to test the model and run it on new images, still directly in the browser.
I would like help constructing a small, free training course based on this content.
The audience is people who are curious how AI is created.
The content should loosely contain: a hook that asks you if you want to create an AI right in your browser (one that you can save and keep); an explanation of the training set editor (which lets you load, save and modify training examples); an explanation of different learning architectures (for example logistic regression works for some position-based image classifications, but you need a neural network to recognize images that depend on relationships between pixels - and the application would let you choose the different architectures and see the effect); a word about the accuracy of the training model (probably based on a test sample set) and finally the execution part, where you show new samples to the model and it classifies them (at this phase, only the user would know if the model is getting things right).
Help me write a course outline. It would be nice if this was formatted as an ImpressJS presentation (I can provvide an example as an index.html file).
