const oneVsAll = function (matrixX, aY, nLabelCount, nLambda) {

    const aMatrixSize = math.size(matrixX);
    const m = aMatrixSize[0];
    const n = aMatrixSize[1];

    const allTheta = math.zeros(m, n);

    return allTheta;
};

export { oneVsAll };